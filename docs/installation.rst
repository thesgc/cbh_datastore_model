============
Installation
============

At the command line::

    $ easy_install cbh_datastore_model

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cbh_datastore_model
    $ pip install cbh_datastore_model
